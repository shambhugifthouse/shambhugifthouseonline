from django.apps import AppConfig

class PersonalServicesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.personal_services'
    verbose_name = 'Personal Services & Expenses'
